{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
    <h1>osoby</h1>
    <a href="{{ path('user_create') }}"/>dodaj nowa osobe </a>
    <table class="table border">
        <tr>
            <td><h3>ID</h3></td>
            <td><h3>imie</h3></td>
            <td><h3>nazwisko</h3></td>
            <td></td>
            <td></td>
            <td></td>

        </tr>
        {% for users in users %}
        <tr>
            <td>{{ users.id }}</td>
            <td>{{ users.name }}</td>
            <td>{{ users.surname }}</td>
            <td>{{ users.iceLover }}</td>


            <td>
                {% if users.category %}
                {{ users.category.name }}
                {% endif %}

            </td>


            <td><a href="{{ path('delete', {'id':users.id}) }}">Usuń</a> </td>
            <td><a href="{{ path('edit', {'id':users.id}) }}">Edytuj</a> </td>
        </tr>
        {% else %}
        <tr>braklo osob</tr>
        {% endfor %}
    </table>
</div>

{% endblock %}